{
  "name": "nebulai-viewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:nebulai": "PSYCHIX_ENTRY=nebulai VITE_BASE=/psychiX/nebulai-maps/ VITE_NEBULAI_APP_URL=/psychiX/nebulai-maps/ VITE_SEER_APP_URL=/psychiX/seer/ VITE_HUB_URL=/psychiX/ VITE_LIVE_URL= VITE_BUILD_URL= VITE_EMBED_HOST= VITE_SEER_URL= vite build --outDir dist/nebulai",
    "build:seer": "PSYCHIX_ENTRY=seer VITE_BASE=/psychiX/seer/ VITE_NEBULAI_APP_URL=/psychiX/nebulai-maps/ VITE_SEER_APP_URL=/psychiX/seer/ VITE_HUB_URL=/psychiX/ VITE_LIVE_URL= VITE_BUILD_URL= VITE_EMBED_HOST= VITE_SEER_URL= vite build --outDir dist/seer",
    "build:hub": "PSYCHIX_ENTRY=hub VITE_BASE=/psychiX/ vite build --outDir dist/hub",
    "build:deploy": "npm run build:nebulai && npm run build:seer && npm run build:hub",
    "preview": "vite preview",
    "test": "vitest run",
    "report": "node --experimental-strip-types scripts/session-report.ts",
    "e2e": "playwright test",
    "e2e:nebulai": "playwright test tests/e2e/nebulai",
    "e2e:seer": "playwright test tests/e2e/seer",
    "e2e:update": "playwright test --update-snapshots",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@deck.gl/core": "^9.3.6",
    "@deck.gl/layers": "^9.3.6",
    "@preact/signals": "^2.9.3",
    "kdbush": "^4.1.0",
    "preact": "^10.29.5",
    "three": "^0.185.1",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.1",
    "@preact/preset-vite": "^2.10.5",
    "@types/node": "^26.1.0",
    "@types/three": "^0.185.0",
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vitest": "^4.1.10"
  }
}
